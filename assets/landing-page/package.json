{
  "scripts": {
    "prepare": "yarn --cwd interactive_slideshow",
    "build": "yarn build:interactive_slideshow && bundler exec middleman build",
    "build:interactive_slideshow": "yarn --cwd interactive_slideshow build --public-url /interactive_slideshow/ --out-dir ../source/interactive_slideshow"
  },
  "devDependencies": {
    "@now/node": "^1.4.1"
  },
  "dependencies": {
    "axios": "^0.19.2"
  }
}
