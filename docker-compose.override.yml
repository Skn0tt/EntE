version: '3'

services:
  mongodb:
    ports:
      - 3500:27017
  reverseproxy:
    build: ./reverseproxy
  api:
    build: ./api
    volumes:
      - ./api/build:/usr/src/app/build
    depends_on:
      - mongodb
      - reverseproxy
  ui:
    build: ./ui
